name: account-service

on:
  push:
    branches: [ main ]
    paths:
      - 'account-service/**'
      - '.github/workflows/account-service.yml'
  pull_request:
    paths:
      - 'account-service/**'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: account-service

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Run tests
        run: ./gradlew test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: account-service-test-results
          path: account-service/build/reports/tests/
